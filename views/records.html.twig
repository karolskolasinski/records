<h1>Records:</h1>
<ul>
    {% for record in records %}
        <li>{{ record.id }}
            {{ record.artist }}
            {{ record.title }}
            {{ record.release_type }}
            {{ record.release_year }}</li>
    {% endfor %}
</ul>

{{ record.id }}
{{ record.artist }}
{{ record.title }}
{{ record.release_type }}
{{ record.release_year }}
